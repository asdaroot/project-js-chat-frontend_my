## 1. Инфраструктура тестов

- [x] 1.1 Установить dev-зависимости: `vitest`, `jsdom`, `@vitest/coverage-v8`, `@testing-library/react` (16.x), `@testing-library/user-event`, `@testing-library/jest-dom` — `npm install` проходит без ошибок
- [x] 1.2 Создать `vitest.config.ts` через `mergeConfig` с react-плагином и блоком `test` (environment `jsdom`, `globals: true`, `setupFiles`) — `npm test` запускается и завершается с кодом 0
- [x] 1.3 Создать `src/test/setup.ts`: подключить `@testing-library/jest-dom`, заглушить `HTMLElement.prototype.scrollIntoView` через `vi.fn()` — setup грузится при запуске любого теста без ошибок
- [x] 1.4 Добавить в `package.json` скрипты `test` (vitest run), `test:watch` (vitest), `test:coverage` (coverage+v8) — скрипты выполняются
- [x] 1.5 Исключить тестовые файлы из продакшен-сборки: `exclude` для `**/*.test.ts`, `**/*.test.tsx`, `src/test` в `tsconfig.app.json`; добавить `tsconfig.vitest.json` (app-конфиг + типы `vitest/globals`, `@testing-library/jest-dom`) — `npm run build` остаётся зелёным

## 2. Юнит-тесты lib

- [x] 2.1 Написать `src/lib/session.test.ts`: save/load/clear, отсутствие сессии, битый JSON, запись без нужных полей — все тесты проходят
- [x] 2.2 Написать `src/lib/api.test.ts` с `vi.stubGlobal('fetch', ...)`: `login` (успех, 401, сеть), `signup` (успех, 409, сеть), `fetchData` (успех, проверка заголовка `Authorization`, ошибки/сеть) — все тесты проходят
- [x] 2.3 Прогнать юнит-тесты `npm test` без необработанных rejections и предупреждений act

## 3. Тесты App (auth, registration, session)

- [x] 3.1 Настроить моки `vi.mock('./lib/api')` и `vi.mock('./lib/socket')` + хелперы рендера для `src/App.test.tsx` — моки подключаются без ошибок
- [x] 3.2 Покрыть вход: дефолтные `admin/admin`, успех → открывается ChatWindow, 401 → «неверный логин или пароль», сетевая ошибка → «не удалось подключиться к серверу» — тесты проходят
- [x] 3.3 Покрыть регистрацию: переключение вкладок, валидация (пустое имя, пароль < 3, пароли не совпали), 409 → «такой пользователь уже существует», успех → ChatWindow — тесты проходят
- [x] 3.4 Покрыть сессию: restore из localStorage при загрузке, logout очищает localStorage и возвращает на форму входа — тесты проходят

## 4. Тесты ChatWindow (ядро capability chat)

- [x] 4.1 Покрыть загрузку/ошибку: «Загрузка…», состояние ошибки + кнопка «Повторить» (повторный `fetchData`), success на повторном fetch — тесты проходят
- [x] 4.2 Покрыть каналы и выбор: сайдбар со списком, дефолтный `currentChannelId`, клик по каналу фильтрует сообщения, пустой канал → empty-state — тесты проходят
- [x] 4.3 Покрыть отправку: emit `newMessage` с `{ body, channelId, username }` и очистка поля; whitespace → эмита нет; `connected = false` → ошибка «нет соединения с сервером» — тесты проходят
- [x] 4.4 Покрыть realtime-входящие: `newMessage` от другого клиента попадает в нужный канал; `newChannel` → в сайдбаре; `renameChannel` → новое имя; `removeChannel` → канал и его сообщения исчезают — тесты проходят
- [x] 4.5 Покрыть диалоги CRUD: create (валидное имя → emit + закрытие; пустое имя → ошибка в диалоге и диалог открыт; `connected = false` → ошибка), rename (валидное/пустое), remove (подтверждение → emit; не-removable канал → кнопки удаления нет) — тесты проходят
- [x] 4.6 Покрыть re-selection: удаление выбранного канала → выбор переходит на первый оставшийся; удаление последнего канала → фиксируем текущее поведение окна чата (без чат-интерфейса) — тесты проходят

## 5. Остальные компоненты

- [x] 5.1 `ChannelSidebar.test.tsx`: рендер каналов, подсветка активного, клик вызывает `onSelect`, кнопка «+» вызывает `onAdd` — тесты проходят
- [x] 5.2 `MessageList.test.tsx`: empty-state при пустом списке, рендер сообщений, `scrollIntoView` вызывается при изменении списка — тесты проходят
- [x] 5.3 `MessageForm.test.tsx`: отправка непустого → `onSend` + очистка; кнопка disabled при пустом/whitespace-тексте; `onSend` вернул false → ошибка — тесты проходят
- [x] 5.4 `ChannelDialog.test.tsx`: фокус на поле при открытии, валидация пустого имени в create/rename, cancel закрывает, подтверждение remove — тесты проходят

## 6. Сверх-спековые тесты и финальная проверка

- [x] 6.1 Тест yank-скролла: `scrollIntoView` вызывается при каждом добавлении сообщения — фиксирует текущее поведение авто-скролла
- [x] 6.2 Тест дедупликации: два входящих `newMessage` с одинаковым `id` попадают в список оба — документирует отсутствие дедупликации как есть
- [x] 6.3 Тест пустого списка каналов: окно чата без каналов работает как сейчас (тест фиксирует факт, поведение не меняем)
- [x] 6.4 Финальная проверка: `npm test` (все зелёные), `npm run build`, `npm run lint`, `npm run test:coverage` (отчёт генерируется) — всё без ошибок и без предупреждений act в консоли